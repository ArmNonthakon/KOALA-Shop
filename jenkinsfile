pipeline {
    agent any
    stages{
        stage("Deploy docker"){
        steps{
            script {
                sh 'sudo docker-compose up -d'
            }
        }
        stage("Down docker"){
            script {
                sh 'sudo docker-compose down --rmi all'
            }
        }
    }
    }
}