pipeline {
    agent any
    stages{
        stage("Deploy"){
        steps{
            script {
                sh 'sudo docker-compose up -d'
            }
        }
        stage("Down"){
            script {
                sh 'sudo docker-compose down --rmi all'
            }
        }
    }
    }
}