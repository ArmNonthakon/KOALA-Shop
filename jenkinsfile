pipeline {
    agent any
    stages {
        stage('Deploy') {
            steps {
                script {
                    sh 'sudo docker-compose up -d'
                }
            }
        }
        stage('Down') {
            steps {
                script {
                    sh 'sudo docker-compose down --rmi all'
                }
            }
        }
    }
}
